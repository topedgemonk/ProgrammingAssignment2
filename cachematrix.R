makeCacheMatrix <- function (mat) {
  
  mat1 <- matrix(rnorm(1000:5000, 400, 400)) #create a square matrix
  
  inv <- solve(mat1) #inverse of matrix
  
}

cacheSolve <- function(mat1){
  
  if inv !== NULL # if inv is not null
  
  return inv } #print inv
else {solve(mat1)
      return(mat1)
}
